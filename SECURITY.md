# 安全策略

这是一个公开仓库。不要提交生产环境密钥或私有基础设施信息。

禁止提交：

- API Key 或比分服务 Token。
- 服务器 IP、SSH Host 别名或 SSH 私钥/公钥。
- 云账号标识。
- 私有部署路径。
- 生产数据库 URL。
- Webhook URL。
- 基础认证凭据。
- 防火墙、主机或生产环境专用运维说明。

使用 `.env.example` 说明需要哪些配置项。真实值只能放在本地 `.env`、服务器密钥管理、部署平台 Secret 或仓库外的私有部署笔记中。

如果密钥被误提交，立刻轮换密钥，并在公开使用前清理 Git 历史。
